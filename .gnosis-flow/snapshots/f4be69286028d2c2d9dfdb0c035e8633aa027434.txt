version: '3.8'

services:
  gnosis-crawl:
    build: .
    container_name: gnosis-crawl
    ports:
      - "8080:8080"
    environment:
      - HOST=0.0.0.0
      - PORT=8080
      - DEBUG=true
      - STORAGE_PATH=/app/storage
      - RUNNING_IN_CLOUD=false
      - GNOSIS_AUTH_URL=http://gnosis-auth:5000
      - MAX_CONCURRENT_CRAWLS=3
      - CRAWL_TIMEOUT=30
      - ENABLE_JAVASCRIPT=true
      - ENABLE_SCREENSHOTS=false
    volumes:
      - crawl_storage:/app/storage
      - ./app:/app/app  # Mount for development hot reload
    networks:
      - gnosis-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  crawl_storage:
    name: gnosis_crawl_storage

networks:
  gnosis-network:
    name: gnosis_network
    driver: bridge