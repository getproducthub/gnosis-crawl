# Mesh coordinator configuration
# Copy these into your .env or set as environment variables

MESH_ENABLED=true
MESH_NODE_NAME=local
MESH_ADVERTISE_URL=http://localhost:8080
MESH_SECRET=changeme-use-a-real-secret
MESH_PEERS=
MESH_PREFER_LOCAL=true
MESH_HEARTBEAT_INTERVAL_S=15
MESH_PEER_TIMEOUT_S=45
MESH_PEER_REMOVE_S=120
MESH_REMOTE_TIMEOUT_MS=35000
